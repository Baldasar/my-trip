<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mytrip" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
<!--        <item name="android:background">@drawable/border_card</item>-->

    </style>

    <style name="CardWithBorder">
        <item name="android:background">@drawable/border_card</item>
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="Theme.Mytrip" parent="Base.Theme.Mytrip" />
    <style name="Theme" parent="Base.CardView">
        <item name="android:background">@drawable/border_card</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary red color for the app -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary</item>
        <item name="colorAccent">@color/primary</item>
    </style>
</resources>